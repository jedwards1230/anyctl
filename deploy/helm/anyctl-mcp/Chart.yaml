apiVersion: v2
name: anyctl-mcp
description: Deploy anyctl's MCP server (streamable-HTTP) as a network-reachable service.
type: application
# version / appVersion are overwritten at publish time from the release tag
# (`helm package --version $VER --app-version $VER`). The committed values are
# placeholders so `helm template`/`helm lint` work locally.
version: 0.0.0
appVersion: "0.0.0"
keywords:
  - anyctl
  - mcp
home: https://github.com/jedwards1230/anyctl
sources:
  - https://github.com/jedwards1230/anyctl
maintainers:
  - name: jedwards1230
