apiVersion: v1
kind: Service
metadata:
  name: {{ include "anyctl-mcp.fullname" . }}
  labels:
    {{- include "anyctl-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "anyctl-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
