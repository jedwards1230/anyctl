{
  "name": "anyctl-mcp-app-view",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Universal adaptive result View for anyctl's MCP Apps integration (table/record/tree renderer)",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "watch": "vite build --watch",
    "dev": "vite"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^2.0.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
